# Аудит М3. Бази даних (позиції 29–30)

> Продовження `migration_plan.md`, крок 3 порядку робіт, і `module_1_audit.md` за тим самим методом. Це аудит, не міграція: жоден файл у `module_3/` не переміщено й не переписано. Назва папки `module_3/` — **оманлива**: вона не відповідає v5.0-модулю «М3. Бази даних» один в один (див. нижче), це лише успадкована назва зі старого репозиторію.

## Мета аудиту

Зрозуміти, що з наявного матеріалу в `module_3/` реально покриває дві цільові позиції v5.0-модуля «М3. Бази даних» (29–30), і окремо — що з решти вмісту цієї папки (алгоритми, структури даних) насправді належить іншим модулям.

## Важлива структурна примітка — розбіжність назви папки й обсягу модуля

v5.0 «М3. Бази даних» — це **лише 2 позиції** (29–30). Але `module_3/lessons/` у новому репо містить старі уроки 22–29 (Big O, deque/queue, списки-як-структура-даних, пошук/хешування, дерева, сортування, графи) — це **алгоритмічний контент**, який за темою належить до практикумів М1/М2 (П1–П6: Big O, пошук, хеш-структури, рекурсія, DP/greedy, stack/queue/heap/trie+LRU — назви практикумів майже дослівно збігаються з назвами цих старих уроків), а не до «Бази даних». Лише хвіст `module_3/` (SQL, PostgreSQL) справді покриває реальну v5.0 М3.

Тому цей документ має дві незалежні частини: §2 — власне аудит 2 позицій М3, §2b — окремий інвентар алгоритмічного контенту як банку джерел для практикумів М1/М2 (не як позицій М3).

## 1. Облік навантаження

v5.0 «М3. Бази даних» — 2 позиції, обидві типу «Урок», без практикумів:

| Тип заняття | К-сть позицій у М3 | Позиції |
|---|---|---|
| Урок | 2 | 29, 30 |
| Практикум (клас) | 0 | — |
| **Разом позицій у М3** | **2** | — |

Алгоритмічний контент `module_3/lessons/lesson_22`…`lesson_28` (7 старих уроків) **не входить у цей рахунок** — це не позиції М3, а потенційний матеріал для практикумів П1–П6 у М1/М2 (див. §2b).

## 2. Аудит уроків М3 (позиції 29–30)

| # і тема | Яку проблему розв'язує | Очікуване вміння | Передумови | Наявні файли та розділи | Повнота покриття | Прогалини й дублювання | Дія | Завдання для перевірки | Питання для викладача |
|---|---|---|---|---|---|---|---|---|---|
| **29.** Основи SQL (PostgreSQL) | Збереження та взаємодія — стан програми поза Python-процесом, у реляційній БД | Пише DDL/DML/DQL, розуміє схему реляційної БД, JOIN-и, підзапити | Позиції 1–28 (М1+М2 повністю) | **`module_3/lessons/lesson_29_sql_basics/note_lesson_29_sql_basics.ipynb`** (29 клітинок, 13 md/16 code) — «Урок 29 — SQL: Від Синтаксису до Архітектури»: DDL (`CREATE TABLE`), DML (`INSERT`/`UPDATE`/`DELETE`), DQL (`SELECT`) на практиці. **`lab_lesson_29_sql_basics.ipynb`** (17 клітинок, 9 md/8 code) — лабораторна робота. **`db_architecture_design.md`** — проєктування реляційних схем (архітектурний рівень, не лише синтаксис). **`diagrams_sql_joins.md`** — mermaid-схеми: 5 підмов SQL, JOIN-и, підзапити. **`duckdb_parquet_datalake.md`** — DuckDB/Parquet/data lake (ширше за «основи SQL», радше аналітичне розширення). Повноцінний застосунок `nyc-taxi-pipeline/` (Docker Compose, API, dashboard, `sql/`, `cypher/`) — виглядає як демонстраційний/лабораторний проєкт поверх цього уроку, не сам урок. Додатково: **`postgresql/note_lesson_30_postgresql.ipynb`** (32 клітинки, 13 md/19 code) — «Урок 30 — PostgreSQL: Архітектура та Практика» — вкладений у папку `lesson_29_sql_basics/`, хоча за власною назвою файлу це наступний, окремий урок (PostgreSQL-специфіка: архітектура, а не лише SQL-синтаксис загалом). | Дуже сильне — теорія, лабораторна, архітектурні діаграми, робочий демо-проєкт | Файл `note_lesson_30_postgresql.ipynb` фізично лежить у папці `lesson_29_sql_basics/`, хоча його власна назва («Урок 30») і зміст (PostgreSQL-архітектура окремо від загального SQL) натякають, що це вже матеріал наступної позиції, не тієї самої. `duckdb_parquet_datalake.md` виходить за межі «основ SQL» (DuckDB/OLAP/data lake) — обсяг, ближчий радше до просунутого/бонусного матеріалу, ніж 2-годинного вступного заняття. `nyc-taxi-pipeline/` — важкий інфраструктурний проєкт (Docker, кілька сервісів) всередині 2-годинної позиції — потребує рішення, чи це частина заняття, чи окремий необов'язковий приклад | Розділити (SQL-синтаксис/DDL-DML-DQL/JOIN — сюди; PostgreSQL-архітектура з вкладеного notebook — імовірно, вже сама позиція 30, не 29; DuckDB/data lake і `nyc-taxi-pipeline/` — оцінити окремо, можливо, поза обов'язковим обсягом) | Лабораторна `lab_lesson_29_sql_basics.ipynb` — готова, використовувана майже без змін | Чи `postgresql/note_lesson_30_postgresql.ipynb` — це вже готовий чорновик самої позиції 30 (а не позиції 29, попри розташування у папці lesson_29)? Якщо так, назва позиції 30 «Redis overview» в xlsx і зміст цього файлу (PostgreSQL) явно не збігаються — див. питання нижче. Чи `duckdb_parquet_datalake.md`/`nyc-taxi-pipeline/` — обов'язкова частина 2-годинної позиції 29, чи винести як необов'язковий поглиблений матеріал? |
| **30.** Redis overview | Збереження та взаємодія — швидкий доступ до стану (кеш, черги, pub/sub) поза реляційною моделлю | Розуміє Redis як key-value/in-memory сховище, базові структури даних Redis, типові сценарії використання (кеш, сесії) | Позиція 29 | **У `module_3/` не знайдено жодного файлу з реальним Redis-контентом.** Пошук `grep -ril "redis" module_3` дав лише хибні збіги — згадки «Redis» побіжно, не як тема уроку (у `docker_guide.md`, у `note_lesson_22_big_O.ipynb`, у діаграмі `lesson_24`, у `lab_lesson_28_graphs.ipynb`) — це не джерело позиції 30. Натомість `module_4/lessons/lesson_30_networking_base/Redis_docs.md` (у сусідньому модулі, не в цьому аудиті — перевіряє паралельний фахівець з М4) має назву, що прямо натякає на призначення для цієї позиції. | Відсутнє в `module_3/` повністю | Немає жодного реального джерела в цій папці — концептуальна прогалина, не деталь | Створити (або перенести/об'єднати з `module_4/lesson_30_networking_base/Redis_docs.md`, якщо той файл справді покриває цю позицію — узгодити з аудитом М4) | Немає готових — усе потрібно створити | **Ключове відкрите питання:** чи `Redis_docs.md` з `module_4/lesson_30_networking_base/` — це фактичне джерело позиції 30 «Redis overview» (номер файлу «30» у назві старої папки це підтверджує), а не будь-що в `module_3/`? Якщо так — стара нумерація тут вводить в оману вдруге (як і в М1: назви папок ≠ v5.0-позиції), і варто зафіксувати це явно, а не шукати Redis-контент у М3. |

## 2b. Алгоритмічний контент у `module_3/` — ймовірний банк вправ для практикумів М1/М2

Це **не позиції М3** — окрема інвентаризація, щоб цей матеріал не загубився між аудитами модулів. Старі уроки 22–28 за темою майже один-в-один збігаються з назвами практикумів П1–П6 з М1/М2.

| Стара тема (`module_3/lessons/`) | Реальний зміст (за читанням) | Ймовірна ціль у v5.0 | Готовність |
|---|---|---|---|
| `lesson_22_big_O` — Big O Notation | «Урок 22 — Big O Notation: Аналіз Складності Алгоритмів» (25 клітинок), три базові правила аналізу, повноцінний Streamlit-лаб `big_o_taxi_lab/` (алгоритми, метрики, датасети таксі) | **М1 позиція 8 (П1: Big O + FizzBuzz/паліндроми/Caesar)** — уже відзначено і в `module_1_audit.md` (рядок 8) як лекційне джерело теорії Big O, не сам практикум | Теорія глибока й готова; самих задач практикуму (FizzBuzz, паліндром) тут немає — узгоджується з висновком М1-аудиту |
| `lesson_23_deque_queue` — Stack, Queue, Deque | «Урок 23 — Stack, Queue, Deque: Архітектура Потоку Даних» (40 клітинок) — ментальні моделі, `list` як стек, **Call Stack і Рекурсія**, перевірка балансу дужок, `collections.deque`, **BFS**. Повноцінний `queue_policy_lab/` (шарувата архітектура: domain/services/infrastructure) | Розділяється між кількома цілями: розділ «Call Stack і Рекурсія» → **М2 позиція 22 (П4: Рекурсія, divide & conquer, backtracking)**; Stack/Queue/Deque сам по собі → **М2 позиція 28 (П6: Stack/Queue/Heap/Trie + LRU Cache)**; BFS перетинається і з `lesson_28_graphs` | Змістовно сильне, але один файл живить щонайменше дві різні позиції М2 — потребує свідомого розділення (як `lesson_25_search_hashing` в М1-аудиті), не механічної нарізки |
| `lesson_24_list_data_structure` — Список як архітектурне рішення | «Урок 24» (36 клітинок) — історія зв'язного списку, клас `LinkedList`, Node-based Queue, **«Завдання 3 — LRU Cache (Рівень: Складний)»** — це прямий, майже готовий матеріал | **М2 позиція 28 (П6: …+ LRU Cache → ООП-пакет)** — LRU Cache тут уже є як завдання, не просто згадка | Сильне пряме влучання; LRU Cache-завдання виглядає майже готовим до адаптації |
| `lesson_25_search_hashing` — Пошук і хешування | Уже проаналізовано в `module_1_audit.md` (позиції 11 і 16) — той самий файл одночасно джерело для М1 П2 (Search) і П3 (Hashing) | **М1 позиції 11 і 16** (уже задокументовано в M1-аудиті) | Без змін відносно висновку М1-аудиту — просто підтверджено тут, що файл фізично лежить у `module_3/`, не `module_1/` |
| `lesson_26_trees` — Дерева та алгоритми дерев | «Урок 26» (20 клітинок) | **Не має явного відповідника** серед названих практикумів П1–П6 у xlsx (жоден з них не називається «дерева») | Готовий, повний матеріал без цільової позиції — відкрите питання нижче |
| `lesson_27_sorting` — Алгоритми сортування | «Урок 27» (21 клітинка), «від Big-O до реального заліза» | **Не має явного відповідника** серед П1–П6 — найближче дотичний до Big O (позиція 8), але сортування там не назване | Готовий, повний матеріал без цільової позиції |
| `lesson_28_graphs` — Теорія графів, BFS/DFS/Dijkstra, Neo4j | «Урок 28» (19 клітинок, 7 частин) — включає `docs_Neo4j.md` (графова БД) | **Не має явного відповідника** серед П1–П6. Neo4j-частина концептуально ближча до «Бази даних» (М3), але не названа в xlsx для позицій 29–30 | Готовий, повний матеріал; Neo4j-частину варто окремо розглянути як можливе розширення М3, а не М1/М2-практикуму |

Явно відсутнє в усьому цьому банку: конкретні задачі для М2 позицій 22 (backtracking/divide & conquer за межами базової рекурсії), 26 (DP + greedy) — жодного файлу з цими темами не знайдено; Heap і Trie (частина назви позиції 28) також не знайдено окремо від LRU Cache/Stack/Queue.

**Поза цим інвентарем, але варто занотувати:** `module_3/docker_guide.md` та `module_3/Docker — від «працює у мене» до production.html` — повноцінний посібник з Docker, явно позначений у власному заголовку «Модуль 3 — Python Advanced» (стара, вже нечинна нумерація модулів). За темою це напряму **v5.0 М5 позиція 48 «Docker»**, не «М3. Бази даних» — і взагалі, `migration_plan.md` §0 explicitly вирішив **не переносити** DevOps-контент старого `module_5` цієї хвилі міграції. Цей файл фізично опинився в `module_3/`, що суперечить обом орієнтирам (не той модуль за темою, і DevOps-контент поки не мав переноситись). `module_3/algoritm_docs.md` — загальний вступний довідник з алгоритмів, не прив'язаний до жодної конкретної позиції; корисний як інструкторський фон для всього банку П1–П6.

## 3. Перевірка конфігурації

- **(а)** `course.yaml`, модуль 3: `path: module_3/lessons`, `lessons: [29, 30]` — жодних вигаданих конкретних файлових шляхів для позицій, лише директорія цілком (так само, як у М1) — перевірено, коректно.
- **(б)** Формулювань «ці 2 уроки вже готові» в конфігах не знайдено — сам факт, що `Redis_docs.md` (ймовірне джерело позиції 30) фізично лежить у `module_4/`, а не `module_3/`, підтверджує, що `course.yaml`'s `path: module_3/lessons` **не покриває** реальне джерело позиції 30 — це варто мати на увазі, коли модуль 3 переходитиме з аудиту в реальний нотбук-контент: можливо, доведеться посилатись на файл з іншої директорії або фізично перенести його (рішення викладача, не цього аудиту).
- **(в)** Назва модуля в `course.yaml` — `"Module 3 — Databases"` — коректно відповідає v5.0 «М3. Бази даних», без завищення обсягу.

## Відкриті питання для викладача

1. Чи `postgresql/note_lesson_30_postgresql.ipynb` (фізично вкладений у папку `lesson_29_sql_basics/`, але названий і за змістом — «Урок 30», PostgreSQL-архітектура) — це вже готовий чорновик позиції **30**? Якщо так, xlsx називає позицію 30 «Redis overview», а не PostgreSQL — це пряма розбіжність назви позиції й наявного контенту, яку варто вирішити явно: або позиція 30 насправді про PostgreSQL-архітектуру (а не Redis, і xlsx застарів), або цей notebook — насправді розширення позиції 29 (SQL/PostgreSQL разом), а Redis для позиції 30 потрібно шукати/створювати окремо.
2. Пов'язане з (1): чи `module_4/lessons/lesson_30_networking_base/Redis_docs.md` — фактичне джерело позиції 30 «Redis overview»? Номер «30» у назві старої папки це підказує. Якщо так, варто зафіксувати явно: джерело позиції 30 лежить у `module_4/`, не в `module_3/`, і, можливо, на етапі реальної міграції контенту (не цього аудиту) файл варто перенести або хоча б явно послатись на нього з М3.
3. `duckdb_parquet_datalake.md` і повноцінний `nyc-taxi-pipeline/` (Docker Compose, кілька сервісів) — це обов'язкова частина 2-годинної позиції 29, чи окремий поглиблений/бонусний матеріал поза базовим обсягом уроку?
4. Алгоритмічний контент `lesson_22`–`lesson_28` (§2b) — чи варто фізично перенести/зіставити з відповідними позиціями М1/М2 практикумів (П1–П6) як їхнє джерело, чи лишити `module_3/` тимчасовим сховищем-банком до окремої сесії аудиту М1-практикумів/М2? Зокрема `lesson_23_deque_queue` одночасно годує щонайменше дві різні позиції (М2 22 і М2 28) — як і `lesson_25_search_hashing` в М1-аудиті, механічно розрізати не варто.
5. `lesson_26_trees`, `lesson_27_sorting`, і Neo4j-частина `lesson_28_graphs` не мають явного відповідника серед названих практикумів П1–П6 у xlsx. Це надлишковий матеріал поза обов'язковою програмою v5.0 (залишити як необов'язковий поглиблений банк), чи практикуми в xlsx описані неповно і насправді мають включати дерева/сортування/графи?
6. `docker_guide.md` і Docker-html у `module_3/` — вони явно про v5.0 М5 позицію 48 «Docker», а не М3, і суперечать рішенню `migration_plan.md` §0 поки не переносити старий `module_5`/DevOps-контент. Чи цей файл потрапив сюди помилково (і його варто прибрати з цього аудиту/модуля до слушного часу), чи це свідомий випереджувальний внесок матеріалу для майбутнього М5?

## 4. Примітка про обсяг

На відміну від М1 (17 позицій, один явно окреслений модуль), цей аудит фактично покрив два незалежні шари: справжню v5.0 М3 (2 позиції, добре забезпечені матеріалом) і набагато більший банк алгоритмічного контенту, що дублює тематику практикумів М1/М2. Останній варто розглядати разом з майбутнім аудитом самих практикумів М1/М2 (§2 таблиць у `module_1_audit.md`/`module_2_audit.md`), а не як частину М3.

## Рішення по позиції 29 (урок створено)

Урок 29 написано з нуля на сюжеті «Смачно + Таксі» (`docs/modules/m3/lesson_29.md`, `module_3/lessons/lesson_29_sql_basics/note_lesson_29_sql.ipynb`) одразу на PostgreSQL (не sqlite3, як старий `note_lesson_29_sql_basics.ipynb`). Зі старих матеріалів узято ідеї: п'ять підмов SQL (DDL/DML/DQL, транзакції), ключі й зв'язки з `db_architecture_design.md`, JOIN-схеми, `RETURNING`, `EXPLAIN`, CTE з `postgresql/note_lesson_30_postgresql.ipynb`. Не перенесено (поза обсягом одного уроку): `nyc-taxi-pipeline/`, DuckDB/Parquet, Neo4j, JSONB, віконні функції, рівні ізоляції, повна нормалізація 1NF–3NF (лише ідея «не дублювати»). Усі результати запитів на сторінці отримано з реального PostgreSQL 16; в ноутбуці — клітинка встановлення PostgreSQL у Colab (`apt-get install postgresql`, перевірено на Ubuntu 24.04).
