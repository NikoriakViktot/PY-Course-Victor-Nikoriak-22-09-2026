# Аудит Mermaid-схем курсу

Правило — `CLAUDE.md`, розділ «Algorithm & architecture diagrams — mandatory»: кожен алгоритм чи керуюча конструкція, яку пояснює урок, має **покрокову схему виконання** на конкретних даних з уроку (`subgraph` на крок, стан у вузлах), а розділи `{#architecture}` — схему архітектурного рішення.

Зразок стилю — старий курс 23_02: `module_3/lessons/lesson_27_sorting/diagrams_lesson_27_sorting.md` (розділ на алгоритм, `subgraph` на прохід, фінальна схема «як обрати»).

Стовпець «Є» — кількість Mermaid-блоків на сторінці `docs/modules/mN/lesson_NN.md` на момент аудиту (2026-09-26).

| Урок | Є | Що вже показано | Чого бракує (покрокові схеми) | Статус |
|---|---:|---|---|---|
| 1 | 0 | — | сторінка-заглушка, алгоритмів немає | не потрібно |
| 2 | 0 | — | сторінка-заглушка | не потрібно |
| 3 | 5 | стан змінних після присвоєння, приведення типів | — | ок |
| 4 | 9 | ланцюжок `elif`, вкладені `if` (шлях для 150 грн), `match`, truthy/falsy, коротке обчислення `and`, цикл `while` + покрокове трасування, `break` покроково, `continue` | — ; `match` — окремий ноутбук `match_case.ipynb` | ок |
| 5 | 4 | два імені — один список, контейнери, **поточний максимум покроково** | — | ок |
| 6 | 3 | підрахунок у словник (схема + **покроково по 5 чеках**), пастка `[[...]] * 3` | — | ок |
| 7 | 4 | виклик і параметри, стек, предикат, декомпозиція | — | ок |
| 8 | 3 | **пари вкладеного циклу покроково проти одного проходу з множиною**, порядок росту, як упізнати складність | — | ок |
| 9 | 2 | `wrapper`, фабрика декораторів | — | ок |
| 10 | 3 | що робить `for`, **пауза генератора на кожному `next()`**, конвеєр генераторів | — | ок |
| 11 | 4 | вибір стратегії, **бінарний пошук `low/mid/high` покроково**, **два вказівники покроково**, ковзне вікно | — | ок |
| 12 | 2 | залежності модулів, кеш `sys.modules` | — | ок |
| 13 | 3 | ієрархія винятків, **два шляхи `try/except/else/finally`**, виняток спливає по стеку | — | ок |
| 14 | 1 | файл → обробка → звіт | — (за потреби: вхід/вихід `with`) | ок |
| 15 | 4 | git-потік, гілки, коміти | — | ок |
| 16 | 3 | зв'язок практикумів, **Two Sum зі словником покроково**, **ключ → хеш → кошик, колізія** | — | ок |
| 17 | 2 | карта модуля, архітектура проєкту | — | ок |
| 18 | 2 | LEGB, конвеєр знижок | — | ок |
| 19 | 5 | клас/екземпляр, пошук атрибута, архітектура | — | ок |
| 20 | 4 | MRO, класи | — | ок |
| 21 | 3 | стани замовлення, публічний API, класи | — | ок |
| 22 | 4 | рекурсивне дерево меню, merge sort, backtracking, вибір підходу | — | ок |
| 23 | 4 | dunder, дескриптори, вибір інструмента, класи | — | ок |
| 24 | 4 | курсори, стани генератора, конвеєр, push/pull | — | ок |
| 25 | 2 | піраміда тестів, мок gateway | — | ок |
| 26 | 5 | часова схема (gantt), greedy покроково, заповнення `dp` покроково, відновлення розкладу, вибір greedy / перебір / ДП | — | ок |
| 27 | 6 | конкурентність проти паралелізму, gantt «послідовно проти потоків», гонитва покроково, пам'ять процесів, цикл подій (sequenceDiagram), вибір інструменту | — | ок |
| 28 | 7 | стек, черга, sift up у купі, префіксне дерево, LRU покроково; classDiagram пакета `dispatch` | — | ок |
| 29 | 6 | клієнт — сервер, ER-схема, порядок виконання `SELECT`, `GROUP BY` покроково, `JOIN` покроково, шари «сервіс — репозиторій — БД» | — | ок |
| 30 | 6 | Redis поруч із PostgreSQL, `GET`+`SET` проти `INCR` покроково, cache-aside (`sequenceDiagram`), черга producer/consumer, вибір «Redis чи PostgreSQL» | — | ок |
| 31 | 8 | шлях запиту URL → DNS → TCP/TLS → HTTP покроково, TCP/TLS-рукостискання і HTTP-обмін (`sequenceDiagram`), що може піти не так (винятки), retry з backoff покроково, по черзі vs `gather`, шари клієнта до API, вибір requests/httpx/aiohttp | — | ок |

Уроки 18–25 перевірено: алгоритми (рекурсія, merge sort, backtracking у 22) і архітектурні рішення мають схеми. Урок 14 — за потреби додати схему входу/виходу `with`.
