# Аудит М2. Python Advanced (позиції 18–28)

> Продовження `migration_plan.md`, крок 3 порядку робіт — той самий метод, що й `module_1_audit.md`. Це аудит, не міграція: жоден файл у `module_2/` не переміщено й не переписано. Назви папок (`lesson_13_*` тощо) використані лише як орієнтир — відповідність до цільових позицій 18–28 встановлена за реальним змістом notebooks, який був прочитаний під час цього аудиту.

## Мета аудиту

Зрозуміти, що з наявного матеріалу `module_2/lessons/` (перенесено зі старого репо, ще не звірено з v5.0) реально покриває кожну з 11 цільових позицій М2, де є прогалини й дублювання. На відміну від М1, тут немає ще жодного викладацького рішення щодо спірного контенту — усі пункти розділу 4 лишаються відкритими.

## 1. Облік навантаження М2

| Тип заняття | К-сть позицій у М2 | Позиції |
|---|---|---|
| Урок | 8 | 18, 19, 20, 21, 23, 24, 25, 27 |
| Практикум (клас) | 3 | 22 (П4), 26 (П5), 28 (П6) |
| **Разом позицій у М2** | **11** | — |

Узгоджується з `course.yaml`'s `modules[1].lessons: [18..28]` (11 елементів) і з підсумковим рядком навігаційної таблиці (46 Урок + 6 Практикум по всьому курсу).

## 2. Аудит уроків М2 (позиції 18–28)

| # і тема | Яку проблему розв'язує | Очікуване вміння | Передумови | Наявні файли та розділи | Повнота покриття | Прогалини й дублювання | Дія | Завдання для перевірки | Питання для викладача |
|---|---|---|---|---|---|---|---|---|---|
| **18.** Функції як об'єкти першого класу | Організація рішення (абстракція над поведінкою) | Передає функції як аргументи, повертає їх з функцій, розрізняє 5 патернів застосування | Позиції 1–17 (особливо 7, 9) | **`lesson_13_functions_first_class/note_lesson_13_functions_first_class.ipynb`** (57K, 29 md/26 code, «Функції як об'єкти першого класу» — назва співпадає з позицією дослівно) + 5 окремих файлів-патернів: `pattern_01_callback.md`, `pattern_02_factory.md`, `pattern_03_decorator.md`, `pattern_04_pipeline.md`, `pattern_05_pure_functions.md`. | Точний тематичний збіг, найсильніше джерело в усьому М2 | М1 позиція 9 (Декоратори) уже містить короткий conceptual bridge «функція — це значення» (вирішено викладачем у `module_1_audit.md`, рядок 9) — тут очікується **повний** розгляд, не повторення того самого вступу. Потрібно перевірити на дублювання зі вступною частиною M1-декораторів, коли обидва уроки будуть фіналізовані. | Об'єднати (notebook як каркас, pattern-файли як розширений довідник/секції) | TODO/завдання всередині notebook — потрібно перевірити на готовність окремо (не вичитано детально в межах цього аудиту) | Чи `pattern_0N_*.md`-файли — це готові розділи для вставки в notebook, чи окремий довідник поза ним (за зразком `module_1/docs/*_docs.ipynb`)? |
| **19.** Класи, простір імен | Інформаційна модель (група пов'язаних даних+поведінки як одиниця) | Визначає клас, `__init__`, атрибути екземпляра/класу, розуміє namespace класу | Позиції 1–18 | **`lesson_15_oop_classes/note_lesson_15_oop_classes.ipynb`** (31K, 17 md/14 code, «Об'єктна модель Python та Класи») — базовий конспект. **`note_lesson_15_titanic_oop.ipynb`** (536K! 33 md/37 code) — паралельний notebook на реальному датасеті (Титанік): 5 завдань (`fare_per_family_member()`, `@staticmethod`-валідація, аналіз виживання дітей, топ-5 дорогих квитків) + фінальна рефлексія «Клас — це не про код». `classes_mental_model.md`, `titanic.csv`. | Дуже сильне — концептуальний конспект + повноцінний прикладний кейс на реальних даних | Titanic-кейс (536K) різко перевищує обсяг 2-годинного заняття — схоже на матеріал, розрахований на кілька занять або факультативне поглиблення | Об'єднати (базовий notebook як каркас) + Розділити (Titanic-кейс — оцінити, скільки з 5 завдань реалістично влазить у 2 год, решта — банк вправ/домашнє) | 5 titanic-завдань — готові, високої якості, з реальними даними | Чи Titanic-кейс продовжується в позиціях 20–21 (є `note_lesson_16_titanic_inheritance.ipynb`) як наскрізний приклад М2 — і чи це свідомий вибір, як чеки кафе в L5→L7 М1? |
| **20.** Наслідування, поліморфізм | Організація рішення (повторне використання поведінки, спільний інтерфейс) | Створює підкласи, розуміє MRO, перевизначає методи, застосовує поліморфізм | Позиція 19 | **`lesson_16_inheritance/note_lesson_16_inheritance.ipynb`** (39K, 16 md/14 code, «Наслідування, MRO та Композиція», включно з розділом «Пастка 2: Несумісні аргументи») + **`note_lesson_16_titanic_inheritance.ipynb`** (341K, 30 md/31 code) — той самий Titanic-кейс, продовжений: MRO як маршрут, множинне наслідування, «Ми змінили не людей — ми змінили систему». Поліморфізм окремо докладно розкритий у **`lesson_17_polymorphism_encapsulation_dunder/`** (обидва файли, «Поліморфізм»-частина). | Наслідування — дуже сильне (2 джерела + Titanic). Поліморфізм — сильний, але фізично лежить у файлах позиції 21, не тут | «Наслідування» і «Поліморфізм» — це одна позиція (20) за навігаційною таблицею, але «Поліморфізм» контентно знаходиться в тих самих файлах, що й «Інкапсуляція» (позиція 21) — реальний розрив між назвою позиції в таблиці й фактичним групуванням старого матеріалу | Об'єднати (наслідування) + Розділити (поліморфізм із файлів lesson_17 — виокремити саме polymorphism-частину сюди, encapsulation-частину лишити позиції 21) | MRO-приклади, Titanic-наслідування — готові | Чи розрізати `lesson_17_polymorphism_encapsulation_dunder` на «поліморфізм» (сюди, позиція 20) і «інкапсуляція+dunder» (позиція 21), чи це недоцільно — аналогічне питання вже поставало в М1 для `lesson_25_search_hashing` і було вирішено «не різати механічно»? |
| **21.** Інкапсуляція, область видимості | Організація рішення (контроль доступу до стану, межі модуля/класу) | Розрізняє public/protected/private конвенції, name mangling, розуміє LEGB-правило області видимості | Позиція 20 | **`lesson_17_polymorphism_encapsulation_dunder/note_lesson_17_polymorphism_encapsulation_dunder.ipynb`** (95K, 40 md/27 code) — «інкапсуляція»-частина: бізнес-приклад нарахування зарплат, LSP, композиція замість наслідування, конвенції іменування, «Name Clashing Bug», повна таблиця дандер-методів. **`lesson_17_cars_oop.ipynb`** (98K, паралельний прикладний notebook, «Автомобілі як Об'єкти», завдання 2–5). | Інкапсуляція й конвенції — сильне покриття (два джерела). Дандер-методи тут ПОВНІ (окрема глибока таблиця + Strategy/Command патерни) | **«Область видимості» (scope, LEGB) не знайдено як окрема тема** в жодному з прочитаних файлів М2 — є лише конвенції іменування (`_protected`, `__private`, name mangling) на рівні класу, не загальне LEGB-правило (local/enclosing/global/built-in). Дандер-методи тут дублюються з позицією 23 (там теж «dunder» за назвою позиції) | Об'єднати (інкапсуляція+конвенції) + Створити (окремий блок про LEGB/scope, якщо він справді відсутній і потрібен саме тут) | Strategy/Command patterns, Free Truffle Bug — готові приклади дебагінгу | Чи LEGB/scope справді планувався тут за задумом навігаційної таблиці, чи «область видимості» мається на увазі вузько (лише public/private-конвенції класу)? Де насправді має жити повний виклад дандер-методів — тут (21) чи на позиції 23, якщо там теж «dunder» за назвою? |
| **22.** П4. Рекурсія, divide & conquer, backtracking — *практикум* | Реалізація та перевірка (розбиття задачі на однакові підзадачі) | Пише рекурсивні функції з базовим випадком, застосовує divide & conquer і backtracking | Позиції 1–21 | **Не знайдено нічого** в `module_2/lessons/` — жодного файлу, що згадує рекурсію, divide & conquer чи backtracking. | Відсутнє повністю | Увесь практикум потрібно створювати з нуля — і теорію, і задачі | Створити (теорія + задачі) | Класичні кандидати: факторіал/Фібоначчі (база рекурсії), Ханойські вежі (divide & conquer), N-ферзів чи генерація підмножин (backtracking) — жодного не знайдено готовим | Чи є джерело цього практикуму деінде поза `module_2/` (напр. в `data/` чи старому репо поза вже перенесеними модулями), чи писати повністю з нуля? |
| **23.** `@property`, декоратори класів, dunder | Організація рішення (контрольований доступ до атрибутів, метапрограмування) | Пише `@property`/`@x.setter`, `@classmethod`/`@staticmethod`, розуміє протокол дескриптора | Позиції 1–22 (особливо 9, 21) | **`lesson_18_property_descriptors/note_lesson_18_property_descriptors.ipynb`** (56K, 25 md/18 code) — дуже глибокий: `@property` (обчислювані атрибути, валідація, пастка рекурсії, антипатерни), `classmethod`/`staticmethod`, повний протокол дескриптора (`__get__`/`__set__`/`__delete__`, data vs non-data), ієрархія пошуку атрибутів (`__getattribute__`), ORM-поля як дескриптори (Django/SQLAlchemy), `dataclasses`/`Pydantic`. **`lesson_18_property_descriptors_final.ipynb`** (49K, 21 md/17 code) — прикладний парний notebook, завдання 2–4 (дескриптор `RangedNumber`, дескриптор для `origin`, фінальна система). `getattribute.md`. | Дуже сильне, найглибше джерело з усього М2 — можливо навіть надлишкове для 2-годинного заняття | Розділ 5 (ORM-поля як дескриптори, `dataclasses`/`Pydantic`-метапрограмування) відповідає радше просунутому/факультативному рівню, не базовій 2-годинній позиції — та сама ситуація, що й з `lesson_19_iterators_generators` у М1-аудиті (глибина понад формат заняття). «Декоратори класів» (class decorators, напр. `@dataclass`) явно окремо не розкрито — є лише «дескриптор»- і «property»-матеріал. Dunder-методи тут не є фокусом (повна таблиця дандерів — у позиції 21-матеріалі) | Розділити (§1–4 — базовий 2-годинний матеріал сюди; §5, ORM/Pydantic — до просунутого треку чи факультативу) + Доповнити (окремий короткий розділ «декоратори класів», якого зараз немає) | Завдання 2–4 з `_final.ipynb` — готові, добре структуровані (fading-scaffolds стиль уже присутній: «Інсайт» → завдання) | Чи «декоратори класів» у назві позиції означає `@dataclass`-подібні готові декоратори, чи написання власних декораторів, що приймають клас (аналог position-9 decorator, але для класів)? Куди дінеться ORM/Pydantic-матеріал розділу 5 — лишити тут як факультатив, чи прибрати з М2 повністю (немає жодної v5.0-позиції під ORM до самого М4)? |
| **24.** Ітератори advanced | Перетворення (лінива обробка потоку даних, просунутий рівень) | `.send()`, ітератор як скінченний автомат, itertools-алгебра (`chain`, `dropwhile`, `islice`), pipeline-патерн | Позиції 1–17 (особливо 10) | **`lesson_19_iterators_generators/note_lesson_19_iterators_generators.ipynb`** (50K, 33 md/21 code) — протокол ітератора, власний клас-ітератор, generator-синтаксис, ментальна модель VCR, генератор як **скінченний автомат**, `.send()`, `chain`, `dropwhile`+`islice`, ETL-патерн чистки даних. **`lesson_19_transactions_streaming.ipynb`** (34K, 15 md/12 code) — реальний кейс потокової обробки біржових транзакцій (HOOK на проблему 5-гігабайтного файлу), NDJSON/CSV стрімінг, pipeline трансформацій. | Дуже сильне й **точно відповідне за глибиною** — це саме той матеріал, який М1-аудит (позиція 10) уже наперед відніс сюди («`.send()`, детальна state-machine модель генератора й просунута itertools algebra переносяться до М2 «Ітератори advanced» (позиція 24)») | Немає значних прогалин — підтверджує правильність рішення М1-аудиту. Єдине: частина базового матеріалу (iterator protocol, простий `yield`) тут дублюється з М1 позицією 10 — навмисне повторення (spaced retrieval) чи випадкове дублювання, потребує розмежування при фіналізації | Об'єднати (theory notebook як каркас, transactions-кейс як практичний приклад) | Transactions-кейс — готовий, реалістичний, добре структурований (13 пронумерованих частин) | Яка саме межа між М1-позицією-10 (фундамент) і цією позицією — чи достатньо явно позначити «RETRIEVE» на початку як «ти вже бачив `yield` на L10, тепер — просунуті патерни», чи потрібно активно вирізати дублікат базового матеріалу з цього notebook? |
| **25.** Тестування з pytest (+ валідація AI-коду) | Реалізація та перевірка (автоматизована перевірка коректності) | Пише `pytest`-тести, фікстури, параметризацію; критично оцінює AI-згенерований код | Позиції 1–24 | **Не знайдено нічого** в `module_2/lessons/` — жодного файлу з `pytest`, `unittest`, тестами чи AI-валідацією коду. | Відсутнє повністю | Увесь урок потрібно створювати з нуля | Створити (теорія pytest + AI-валідація) | — | Чи є джерело деінде (module_3/module_4, чи поза перенесеними модулями) — за межами цього аудиту (лише module_2 перевірено), варто перевірити окремо. «AI-валідація коду» — це продовження політики з `migration_plan.md` §1.3, чи окрема тема? |
| **26.** П5. Динамічне програмування + greedy (вступ) — *практикум* | Реалізація та перевірка (оптимізація через мемоізацію/жадібний вибір) | Розпізнає задачі під DP (перекриття підзадач), пише memoization/tabulation, розрізняє DP vs greedy | Позиції 1–25 (особливо 22) | **Не знайдено нічого** в `module_2/lessons/`. | Відсутнє повністю | Увесь практикум потрібно створювати з нуля | Створити (теорія + задачі) | Класичні кандидати: Фібоначчі з мемоізацією (перехід від 22-рекурсії), задача про рюкзак чи «монети» (DP), задача про видачу решти (greedy) — жодного не знайдено готовим | Чи П4 (22, рекурсія) і П5 (26, DP+greedy) варто проєктувати як одну наскрізну пару вправ (рекурсія → мемоізація → tabulation), за аналогією з L6→L7 (звіт кафе → функції) у М1? |
| **27.** Потоки, multiprocessing, asyncio: вступ | Організація рішення (паралелізм і конкурентність, базовий рівень) | Розрізняє threading/multiprocessing/asyncio, розуміє GIL на базовому рівні, пише простий `async def` | Позиції 1–17 | **Не знайдено нічого** в `module_2/lessons/`. **Побічно виявлено під час перегляду сусідніх папок (поза межами цього аудиту, лише для довідки):** `module_4/lessons/lesson_32_threading_concurrency/`, `lesson_33_multiprocessing/`, `lesson_34_asyncio/` — існують, але це прикладний/просунутий рівень (multithreaded scraper, flood-processing pipeline, FastAPI+asyncio) у контексті М4/Web Advanced, не «вступ» рівня М2. | Відсутнє в М2 повністю; наявний споріднений матеріал в іншому модулі (М4) не відповідає заявленому в позиції 27 рівню «вступ» | Позиція явно потребує **вступного**, ізольованого від Web-контексту викладу threading/multiprocessing/asyncio — того, що є в М4, недостатньо як базове джерело (воно й фізично поза модулем, і за рівнем — прикладне) | Створити (вступний теоретичний матеріал, окремо від М4-контенту) | — | Чи можна переиспользовувати щось із М4-notebooks як «просунуте продовження» цієї теми (М2 вступ → М4 практика), чи це вводить небажане випередження контенту з М4 у М2? Це рішення, аналогічне М1-позиції-10 vs М2-позиції-24, тому й фіксується тут явно. |
| **28.** П6. Stack/Queue/Heap/Trie + LRU Cache → ООП-пакет (артефакт М2) — *практикум* | Реалізація та перевірка (структури даних як інструмент ефективності) + синтез усього М2 в один артефакт | Реалізує stack/queue/heap/trie, LRU cache; збирає ООП-пакет, що демонструє М2 | Позиції 18–27 (весь модуль) | **Немає прямого відповідника** структурам даних (stack/queue/heap/trie/LRU). Але **`lesson_21_module2_review/note_lesson_21_module2_review.ipynb`** (68K, 16 md/13 code, «Фінальний Проект: Mini E-commerce Analytics Engine») — це готовий **синтез-артефакт** старого М2: 10 частин від функцій-як-аналітичних-одиниць → пайплайн трансформацій → декоратори-як-інфраструктура → класи → `@property`/дандер/інкапсуляція → наслідування → композиція → context manager (`DataLoader`) → Streamlit-дашборд. `ecommerce_analytics/` — повний робочий пакет (Streamlit-застосунок, `core/analyzer.py`, `core/data_loader.py`, реальні дані `amazon_ecommerce_1M.csv`). | Структури даних — відсутні повністю. «ООП-пакет (артефакт)»-частина позиції — дуже сильна, готовий приклад саме такого синтезу (аналогічно L17 CLI-проєкту в М1) | Позиція фактично об'єднує дві незалежні вимоги (алгоритмічний практикум + підсумковий артефакт), і старий матеріал покриває лише другу. Немає жодного stack/queue/heap/trie/LRU-файлу | Створити (stack/queue/heap/trie/LRU — з нуля) + Об'єднати (review-notebook як прообраз «ООП-пакета», адаптувати структуру, не механічно копіювати e-commerce-тему) | `ecommerce_analytics/` — готовий робочий приклад архітектури пакету (можна показати «як має виглядати» фінальний артефакт) | Чи «ООП-пакет» (артефакт М2) — це буквально той самий е-commerce-кейс під новою назвою, чи новий проєкт, що включно демонструє ще й stack/queue/heap/trie/LRU з цієї позиції? Це та сама логіка «зверху вниз: задача → дані → …», яку М1-аудит зафіксував для L17 CLI-проєкту (`calculator_project` відхилено як недостатній) — чи `ecommerce_analytics` теж варто відхилити з тієї ж причини? |

## 3. Перевірка конфігурації

- **(а) `course.yaml`'s `lessons: [18..28]` для М2 не видає неперенесений контент за доступний.** Той самий загальний коментар у `course.yaml` («Module/lesson numbers below are v5.0 TARGET numbers… They do NOT yet map to the lesson_NN_* folder names…», рядки 21–25) стосується й М2, і цей аудит підтверджує чому: реальна відповідність «стара папка → нова позиція» тут теж майже ніде не 1:1 (позиція 20 «Наслідування, поліморфізм» фізично розкидана між `lesson_16` і `lesson_17`; позиції 22, 25, 26, 27 не мають жодного файлу-відповідника).
- **(б) Немає вигаданих шляхів.** `course.yaml` для модуля 2 вказує лише `path: module_2/lessons` (одне посилання на директорію) — перевірено, конкретних `lesson_NN_*`-шляхів для відсутніх позицій (22, 25, 26, 27) не перелічено, вигаданих файлових шляхів немає.
- **(в) `title: "Module 2 — Python Advanced"`** — нейтральна LMS-назва, не робить заяв про готовність контенту; коректна без змін.

## 4. Відкриті питання для викладача

Жодне з наведених нижче питань ще не вирішено — на відміну від М1-аудиту, це перший прохід по М2, і всі рішення щодо спірного контенту залишаються за викладачем.

1. Позиція 18 — чи `pattern_01–05_*.md`-файли стають розділами всередині notebook, чи окремим довідником поза уроком (за зразком `module_1/docs/*_docs.ipynb`)?
2. Позиція 19 — чи Titanic-кейс (536K, 5 завдань) продовжується в позиціях 20–21 як свідомий наскрізний приклад М2 (аналогічно чекам кафе в L5→L7 М1), чи це лише банк вправ без наскрізної ролі?
3. Позиція 20 vs 21 — `lesson_17_polymorphism_encapsulation_dunder` фізично містить і «поліморфізм» (позиція 20), і «інкапсуляція+dunder» (позиція 21) в одному файлі. Розрізати механічно на дві частини, чи лишити як інструкторське джерело для обох позицій без розділення файлу (за аналогією з рішенням М1 щодо `lesson_25_search_hashing` — «не різати механічно»)?
4. Позиція 21 — чи «область видимості» справді означає LEGB-правило (local/enclosing/global/built-in), якого зараз ніде немає в М2-матеріалі, чи вужче — конвенції `_protected`/`__private` на рівні класу (яке вже є)?
5. Позиції 21 і 23 обидві названі частково через «dunder» — де насправді має жити повний виклад дандер-методів: позиція 21 (де він зараз фізично є) чи позиція 23 (де назва позиції теж його обіцяє)?
6. Позиція 22 (П4, рекурсія) — немає жодного джерела в `module_2/`. Створювати повністю з нуля, чи є джерело деінде (не перевірено за межами `module_2/` у цьому аудиті)?
7. Позиція 23 — «декоратори класів» у назві позиції: це `@dataclass`-подібні готові декоратори, чи написання власних декораторів для класів? Куди дінеться просунутий ORM/`Pydantic`-матеріал (розділ 5 у `note_lesson_18_property_descriptors.ipynb`) — факультатив, чи прибрати з М2 повністю?
8. Позиція 24 — межа з М1-позицією-10: чи потрібно явно вирізати з цього notebook базовий iterator-protocol матеріал, що вже дубльований на L10, чи позначити його як навмисний RETRIEVE-вступ?
9. ~~Позиція 25 (pytest) — немає жодного джерела в `module_2/`.~~ — **вирішено:** джерело знайдено в старому `module_5/lesson_Django_Testing/` (~10 тис. рядків). Розподіл за рішенням викладача: чисто-Python-частина (TESTING_FOUNDATIONS, UNITTEST_BASICS, PYTEST_BASICS, TEST_DATA_AND_FIXTURES, MOCKING_AND_PATCHING) — основа уроку 25 на історії доставки, `basics/` перенесено як є в `module_2/lessons/lesson_25_pytest_testing/basics/`; Django-частина (DJANGO_TESTING, TESTING_PRACTICE_PROJECT, `crispy_notes_project`) — до уроків 33–34; SELENIUM — до уроку 34 чи 41; тестування API — урок 41; CI_CD — урок 50. «Валідація AI-коду» в уроці 25 — тести на інваріанти й чекліст перевірки AI-коду; ширша AI-тема — урок 42.
10. ~~Позиція 26 (П5, DP+greedy) — немає жодного джерела. Проєктувати як пару з П4 (рекурсія → мемоізація → tabulation), за аналогією з L6→L7 (звіт кафе → функції) у М1?~~ — **вирішено:** створено з нуля (`docs/modules/m2/lesson_26.md`, `module_2/lessons/lesson_26_practicum_dp_greedy/`). Так, пара з П4: `fib` + `lru_cache` з уроку 22 → рекурсія `best(i)` для зважених інтервалів (35 421 виклик без кешу → 21 з `lru_cache`) → табуляція. Сюжет — вибір замовлень водієм (interval scheduling: greedy для кількості, ДП для заробітку); монети/рюкзак свідомо не в основній частині. `re` — додатковий ноутбук.
11. ~~Позиція 27 (потоки/multiprocessing/asyncio, вступ) — немає джерела в М2; споріднений матеріал у М4 (`lesson_32`–`34`) — прикладний/просунутий, не «вступ». Створювати ізольований вступний матеріал з нуля, чи є ризик небажаного випередження контенту з М4?~~ — **вирішено:** створено ізольований вступ з нуля (`docs/modules/m2/lesson_27.md`, `module_2/lessons/lesson_27_concurrency_intro/`). Старі M4-уроки 32–34 (23_02) використано лише як джерело ідей (гонитва/Lock, GIL, ізоляція процесів, цикл подій); їх прикладна частина (scraper, flood pipeline, FastAPI) не переноситься — в v5.0 це уроки 31 (httpx/aiohttp) і 37–38 (FastAPI).
12. Позиція 28 (П6, структури даних + артефакт) — `ecommerce_analytics/` (з `lesson_21_module2_review`) — готовий приклад «ООП-пакета», але без жодного stack/queue/heap/trie/LRU-контенту. Це основа для фінального артефакту (як `calculator_project` розглядався й відхилено для L17 М1), чи новий проєкт потрібен з нуля, що вже включає ці структури даних?

## 5. Примітка про потік

Не перевірялось окремо в цьому аудиті — див. `module_1_audit.md` §6 і `migration_plan.md` розділ 4 щодо непідтвердженого коду потоку/дати старту в `course.yaml`.
